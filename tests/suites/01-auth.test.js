// tests/suites/01-auth.test.js
//
// STUB — not yet implemented. See testing guide/02-api-test-architecture-plan.md
// §6 for the endpoint coverage this suite owns:
//   register, login, logout (current/all), refresh, sessions (list/revoke),
//   otp send/verify, /me, google callback (stubbed)
//
// describe.skip (not a bare empty file) so this suite reports as "skipped"
// rather than failing Jest's "must contain at least one test" check, and so
// `npm test` output makes it obvious this suite is pending, not passing.

describe.skip("auth", () => {
	test("TODO: implement", () => {});
});
